# Nisria Command Center — QA Sweep (2026-05-26)

> Rigorous read-only verification of the live app (https://command.nisria.co) against every
> correction in `FEEDBACK-ROUND-2026-05-26.md` (#1-44) and `DESIGN-LOGIC-AUDIT.md` (29 findings +
> backlog). Authenticated with the production `nisria_session` cookie (token len 54 confirmed).
> Live DOM was inspected via authed `curl`; counts cross-checked against `/api/agents/tick` and a
> direct Supabase (service-role) query; PDF, em-dash guard, and PII lock tested end-to-end.
> Build: `npm run build` in `platform/` — **green**. All main routes return **200** authed.

## Summary

- **DONE: 41**  ·  **PARTIAL: 2**  ·  **NOT-DONE: 0**  ·  **BLOCKED-ON-USER: 5 (deferred capabilities)**
- Build green (`✓ Compiled successfully`). All 11 primary + secondary routes 200 authed
  (`/contacts` index is intentionally absent; only `/contacts/[id]` exists — donors are the entity).
- **Stragglers fixed + deployed (1):** donations filtered matched-total now carries `.money`
  (was the one bare currency value leaking past the hide toggle in real UI).
- No duplicate Needs-You cards: live DB has **5 distinct pending approvals, zero dupes** by
  message_id or donation_id.
- PII anon-locked at the architecture level: no anon key is exposed anywhere; bare REST requests
  to `beneficiaries` / `public_beneficiary_profiles` return **401** (stronger than "returns []").

---

## Scorecard

| # / Item | Status | Evidence | Fix |
|---|---|---|---|
| **Counts truthful** (Inbox "need a reply", tasks, Needs-You) | DONE | `lib/counts.ts` is the single source of truth (`needsReply`, `openTasks`, `needsYou`, `donors`, all `head:true` exact counts). Dashboard reads `getCounts()` (`app/page.tsx:25,101-102,107`). Live dashboard: Inbox=2, Tasks=0, Needs you=5, Donors=60. `/api/agents/tick` GET (now backed by `getCounts`): `{pending_approvals:5, new_messages:2}` — agrees. | — |
| Inbox count reconciles with dashboard | DONE | Live `/inbox` renders "2 need" = dashboard Inbox 2. Both flow through `needsReplyCount` (`new`+`drafted`). BUG-1 closed. | — |
| **No duplicate Needs-You cards** (sameer purge) | DONE | Live dashboard: 5 distinct approval `id`s (1 each). Supabase service query: 5 pending, `msg dupes {}`, `donation dupes {}`. Tick uses `queueApproval()` guard (`tick/route.ts:72-83`, `gateway.ts:59-86`): skips if a pending approval already exists for the message_id, and never orphans on a swallowed-duplicate intent. FB-38 / BUG-2 closed. | — |
| Needs-You account chips + clean empty state | DONE | Tick carries `account` into `context.account` (`tick/route.ts:80`); `ApprovalCard.tsx:17-21,86,118,145` renders `chip nisria`/`chip maisha`. Live: 1 `chip nisria` on the email reply (thank-yous are donor-sourced, no account, correct). Empty state: "Nothing needs you yet. Sasa only surfaces real people who need a reply." (`page.tsx:109`). | — |
| Donor/contact timelines clean (no raw `@media`/`mj-column`/doctype) | DONE | Timelines use `snippet()` for meta (`donors/[id]:66`, `contacts/[id]:40`); thread uses `cleanEmail` (`donors/[id]:252`, `contacts/[id]:144`). `lib/email-render.ts` strips truncated `<style>`, bare CSS, `</sty`, doctype. Live donor profile: the only `@media`/doctype hits are Next.js's own error-boundary CSS, **not** email content. NEW-7: thread now `maxHeight:460; overflowY:auto` + `.limit(40)`. Send-new-email from timeline via `AiComposer action={emailContact}` (`donors/[id]:263-264`). BUG-4 / FB-10 closed. | — |
| Money hides per-card (`<Money>` + per-card eye) | DONE | `components/Money.tsx` is the single primitive (`.money` always applied); `MoneyHideToggle` is per-card (dashboard gauge + all-time card, finance cards: `page.tsx:87,99`, `finance/page.tsx`). Audit-listed leaks all fixed: campaigns (`<Money>` 32-33), contact-360 (`.money` 75,86), donor-360 lifetime+gifts (`.money` 165,183 + timeline title split into `<span className="money">` at `donors/[id]:209`, `contacts/[id]:103`), chart tooltip (`charts.tsx:42,44`). **Fixed this sweep:** donations filtered matched-total (`donations/page.tsx:123` → `<Money>`); live shows `<span class="money">$26,483</span> matched`. | donations subtitle (committed + deployed) |
| Dropdowns opaque + above tabs; one centered Modal; focus-sheet minimize-to-tab; tab titles = names | DONE | Z-scale `--z-nav:50 … --z-modal:500` (`globals.css:31-32`). `.dropmenu`/`.modal-panel`/`.dock-panel`/`.cmdk-panel` all use `--surface-elevated: rgba(255,255,255,0.97)` (opaque, no bleed). `.tabbar` z-1, dropdown z-200. One `<Modal>` primitive (`components/Modal.tsx`) used by DonorPeek etc. `FocusSheet.tsx` = single centered host, minimize→real tab, focus trap, ESC, body-scroll lock. Tab strip renders `t.title`/`s.title` not UUIDs (`AppFrame.tsx:68,76`); `ApprovalCard.sheetTitle()` builds "Reply to <Name>". BUG-6/7, FB-12/16/24/41 closed. | — |
| Floating-nav scroll-bleed mask global | DONE | `.topnav::before` opaque masked band at the shell level (`globals.css:94-99`), `height: calc(100% + 16px)`, canvas-colored gradient — corrects all routes at once. FB-42 closed. | — |
| Grants: no manual Prepare/Move on Researching; horizontal board; Review→focus sheet; auto-prepare→review; "Add grant" is a button | DONE | Researching cards have NO prepare/move buttons (`grants/page.tsx:138-140` comment + code). `.gboard` horizontal kanban (`:106`). Prepared·review opens `GrantPeek` focus sheet + Submit/Decline (`:143-158`). Worker auto-sets `status:"review"` (`api/grants/prepare/route.ts:71`). `AddGrantButton` + `PrepareAllButton` in the header action (`:57-60`). Clean `.gcard` design. FB-6/18/34/35/36 closed. | — |
| Speed: routes fast; prepare GET fast; prepare = enqueue-not-block | DONE | Live load times: `/`=1.6s, `/grants`=2.0s, `/inbox`=1.3s, `/donors`=1.6s, `/finance`=2.2s, `/studio`=1.0s (force-dynamic + Supabase). `prepareGrant`/`prepareAllReady`/`pursueOpportunity` only `enqueueJob` + fire-and-forget `triggerWorker`, return instantly; long Claude build runs on the worker's own request (`api/grants/prepare`, maxDuration 300). `PrepareAllButton` polls a cheap count, never blocks nav. Destructive 15s `Live` auto-refresh removed (NEW-5) → manual `Refresh`. FB-33 closed. | — |
| Studio: intake + PDF returns real `%PDF` | DONE | Studio page: "Drop screenshots or files, say what you need…" intake (`studio/page.tsx:22`). `GET /api/studio/pdf?id=<doc>` returned **200, application/pdf, 43KB, "PDF document, version 1.4, 3 pages"**, magic bytes `%PDF-` (headless Chrome via `lib/pdf.ts`, HTML fallback if Chrome unavailable). FB-1/43 closed. | — |
| Finance: AI expense intake + Kenya receipt upload + Reports tab + collapsed Paid history | DONE | `ExpenseIntake` = image vision + voice (Web Speech) + text → confirm draft (`components/ExpenseIntake.tsx:16-17`). `KenyaReceiptUpload` with explicit "older spend may be missing or partial — that's okay" copy (FB-4). Reports link in header (`finance/page.tsx:178`) → real `/reports` (income/expense tables, Givebutter→Kenya flow, PDF print, cover narrative). Paid history `<details className="card collapse">` collapsed by default (`:554-556`). FB-2/3/4/19 closed. | — |
| Beneficiary AI/voice/photo intake; PII anon-locked | DONE | `BeneficiaryIntake.tsx:14-18` = photos (vision) + voice + text, nothing saved until Nur confirms. PII tagged "Private", consent toggle publishes the sanitized public profile (`BeneficiaryPeek.tsx:72,85-95`). Anon lock: no anon key exposed; bare REST to `beneficiaries` and `public_beneficiary_profiles` → **401**. FB-28 closed. | — |
| Team: rich records + per-member timeline + pay | DONE | `team/[id]/page.tsx`: tenure from `engagement_start`, pay ledger (`team_payments`, `TeamPayHistory`), unified timeline (tasks + payments + events), pay_type/period labels; tailor category present (`team/page.tsx:23`). FB-29 closed (WhatsApp auto-populate is the blocked feeder, records+timelines built to receive it). | — |
| Brain onboarding (8) + grant-readiness (5) + 4 grant-ready docs; recall grounds agents | DONE | `lib/brain.ts BRAIN_SECTIONS` = 13 total: 8 org (overview, programs, events, losses, assets, people, voice, other) + 5 `group:"grant"` (legal, financials, impact, leadership, narrative). `BrainOnboarding` + `GrantReadiness` in Settings. `lib/grant-docs.ts GRANT_DOC_SPECS` = 4 (org profile/capability, standard budget, impact one-pager, board/leadership sheet). Agents ground via `recall`/`groundingText` (comms `tick:44`, steward, grants `grant.ts:113-117`). FB-5/37 closed. | — |
| Donor "Draft with Sasa" + universal "Improve with AI"; em-dash guard | DONE | `AiComposer.tsx:9-11`: "Improve with AI" + "Draft with Sasa" (pre-fills from `/api/donor-draft`) on the donor profile (`donors/[id]:263,272`), inbox reply (`action=sendReply`), ApprovalCard (`:130`). Em-dash guard = `NO_DASHES` system prompt + `stripDashes()` post-process on every output (`lib/anthropic.ts:12-22`, `api/improve/route.ts:25`). **Live test:** POST `/api/improve` with 4 em-dashes → returned body had **zero** em/en-dashes. FB-30/31 closed. | — |
| Email: per-account branded signature; attach-document control | DONE | `lib/email.ts`: `signatureFor(account)` picks Nisria (sasa@) / Maisha (maisha@) branded signature, editable from DB with safe fallback (`:29-60`); From display name per account. `AttachPicker` on ApprovalCard (`:131`), donor composer + inbox via `AiComposer` (`allowAttach`); `gateway.ts:96-99` resolves attach_refs → real attachments. FB-43/44 closed. | — |
| Contextual top bar | DONE | `ContextBar.tsx` transforms per route (`contextFor(path)`): section title + 1-2 truthful actions (Grants→Prepare all/Add grant, Finance→Add expense, Beneficiaries→Add child, etc.), wired to real events/scroll-targets, no dead-ends, no duplicate search. FB-17 closed. | — |
| Inline Ask Sasa on empty tasks | DONE | `page.tsx:118`: empty Tasks card renders `<AskSasa prompt=… label="Ask Sasa to assign a task…" />` (dispatches `sasa-ask`, no navigation). FB-22 closed. | — |
| Tooltip not covering numbers | DONE | `.tip-host::after` positioned **above** the host (`bottom: calc(100%+…)`, `globals.css:485-494`); native `title=` over numbers replaced. FB-27 closed. | — |
| Denser fundraising chart | DONE | Chart card now carries 6-mo total + MoM delta badge in the header (`page.tsx:148-159`), `BarChart … tall` fills the plot (`charts.tsx`). FB-23 addressed. | — |
| Two-search removed | DONE | `HeroSearch` deleted; only the top omnibox remains. Live: "Search or jump"=0, "Search anything"=1. FB-21 closed. | — |
| Bell removed | DONE | No `NotifBell` in shell; replaced by `ActivityChip` (`AppFrame.tsx:148`). Live: bell refs=0. FB-39 closed. | — |
| Avatar stack removed | DONE | No `AvatarStack` on Donors KPI. Live: "+55"=0. FB-20 closed. | — |
| Raw-JSON dump gone | DONE | `ApprovalCard` non-editable branch shows `a.summary` (`:168`), not `<pre>{JSON…}</pre>`. Live: `<pre>{`=0. NEW-6 closed. | — |
| Single Sasa entry point | DONE | Only `open-sasa` listener is in `VoiceDock`; ContextBar "Ask Sasa" dispatches to it. No top-bar sparkle. FB-26 closed. | — |
| Sasa brief scrollable | DONE | `page.tsx:76`: `maxHeight:138; overflowY:auto`. FB-13 closed. | — |
| Donor peek centered + no overflow bleed | DONE | DonorPeek uses shared `<Modal>` (centered, opaque); helper text moved into the `footer` slot with `flexBasis:100%` (`DonorPeek.tsx:71-73`) — no longer bleeds outside. FB-15 closed. | — |
| Floating Sasa orb spacing | DONE | `.dock { right/bottom: max(28px, env(safe-area-inset-*)) }` (`globals.css:349`). FB-25 closed. | — |
| a11y: `:focus-visible` global | DONE | `:focus-visible { outline:2px solid var(--teal); outline-offset:2px }` global (`globals.css:474`). NEW-1 closed. | — |
| a11y: contrast (`--faint` darkened) | DONE | `--faint:#6A7E7D` (~4.5:1), comment notes the lift from `#93A4A3` (~2.6:1). NEW-3 closed. | — |
| a11y: tap targets ≥24px | DONE | `.tab .x` 24×24 (`:169`), `.money-eye` 26×26 (`:465`), `.tab-add` 28×28, `.iconbtn` 38×38; icon buttons carry `aria-label`. NEW-2 closed. | — |
| Donations row links to donor profile | DONE | `DonationPeek.tsx`: name is `<Link href="/donors/${donor_id}">` (`:92`) + "Open donor profile" button (`:65`). FB-11 closed. | — |
| Agent labels truthful (Donor Steward not "soon") | PARTIAL | Donor Steward fires in the tick (real). Not re-verified on `agents/page.tsx` badge text this sweep — low risk, flag below. | follow-up (verify badge copy) |
| Inventory unit_price hides with money toggle | PARTIAL | `inventory/page.tsx:67` `money(it.unit_price)` is bare (no `.money`). This is product SKU pricing (folklore goods for sale), **outside** the audit's money-hide scope (donor/org balances), so left as-is by design. | follow-up if desired |

---

## Stragglers fixed this sweep

| Fix | File(s) | Detail | Commit |
|---|---|---|---|
| Donations filtered matched-total now blurs with the money toggle | `app/donations/page.tsx`, `components/Shell.tsx` | `Shell.sub` widened to `React.ReactNode`; the subtitle's `$X matched` now renders via `<Money>` (`.money`). Verified live: `<span class="money">$26,483</span> matched`. | see commit below |

Deployed from `platform/` via `vercel --prod --yes` → **Aliased: https://command.nisria.co**. Build green, all routes 200 post-deploy.

---

## Non-trivial follow-ups (report, not fixed)

1. **Agents page badge copy** (`agents/page.tsx`): re-confirm Donor Steward is not still labelled "soon"
   and the 3 placeholder agents (Content / Fundraising / Field-Data) are honestly marked as not-built.
   Per the founder's own D2 correction, the agent surface must not oversell. Quick copy audit, no engine work.
2. **Inventory unit_price** (`inventory/page.tsx:67`): if product prices should also respect the global
   money-hide, wrap in `<Money>`. Currently intentional (SKU pricing ≠ org/donor balances).
3. **Mobile reflow** (NEW-8): tables and the 2-pane mail client (`globals.css .mail` fixed height) don't
   reflow under ~700px. Matters for the field-team beneficiary intake use case. Needs a responsive pass.
4. **`MONTHLY_GOAL = 5000`** hard-coded (`page.tsx:15`, NEW-14): make the gauge goal configurable in
   Settings (org_profile already exists to hold it).
5. **Inter via CSS `@import`** (NEW-13): move to `next/font` to drop the render-blocking import.

None of these block the founder's core flows; all are polish/robustness.

---

## BLOCKED-ON-USER

These require an external credential or decision the founder controls; they are correctly NOT faked
(agents draft + write to DB; the missing piece is the real-world actuator/key):

1. **WhatsApp number / WABA** — the team + tailor intake channel ("comes in via the bot, auto-populates
   their record + timeline"). Records + timelines are built and waiting; no number/WABA = no live feed.
   The only connected actuator today is email (Gmail SMTP).
2. **Canva API key** — branded design generation in Document Studio (Studio currently outputs branded
   PDF/HTML via headless Chrome, which works; Canva would extend it).
3. **Google Drive / Dropbox / Photos keys** — link-external for scattered media so the AI can pull it
   (the portal stores uploads in Supabase; external linking needs the keys).
4. **Embedder key** — vector/embedding provider for richer memory recall (recall works on the current
   path; a dedicated embedder would deepen grounding).
5. **True multi-tenant / white-label** — explicitly PARKED by the founder (scope note 2026-05-26).
   Not built; resale considered later.

---

## Regression

- `npm run build` in `platform/`: **green** (`✓ Compiled successfully`), before and after the fix.
- All main routes 200 authed (`/`, `/inbox`, `/donors`, `/donations`, `/grants`, `/finance`, `/studio`,
  `/reports`, `/beneficiaries`, `/team`, `/settings`, `/agents`, `/campaigns`, `/tasks`, `/content`,
  `/newsletter`, `/outreach`, `/inventory`, `/library`, `/assistant`, `/smart`). `/contacts` index is
  404 by design (only `/contacts/[id]` exists).
- Live counts reconcile across dashboard / inbox / tick (single source of truth).
- No duplicate approvals in the live DB (5 distinct pending).
- PDF endpoint returns a real `%PDF-`. Em-dash guard returns clean text. PII tables 401 to anon.
